# Use root/example as user/password credentials
version: '3.1'

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - '27017:27017'
    volumes:
      - dbdata6:/data/db
    restart: always
    env_file:
      - .env
  # rust:
  #   image: rust_up_in_here
  #   depends_on:
  #     - mongo
  #   ports: 
  #     - '8080:8080'
  #   env_file:
  #     - .env
  #   restart: unless-stopped
    
volumes:
  dbdata6: